apiVersion: v2
name: teupadel-processor
description: Wrapper chart — padel pose estimation (GPU) via the shared generic-app chart
type: application
version: 0.1.0

dependencies:
  - name: generic-app
    version: "0.5.0"
    repository: "oci://ghcr.io/cmoreira-dev/charts"
